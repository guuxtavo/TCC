export type AvailableWorkspace = {
   numerosIndisponiveis: number[],
   funcionarioFinal: {
      id: number,
      nome: string
   }[],
   funcionarioArmador: {
      id: number,
      nome: string
   }[],
   funcionarioAssenteiro: {
      id: number,
      nome: string
   }[],
   funcionarioEncosteiro: {
      id: number,
      nome: string
   }[],
}