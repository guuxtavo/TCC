export type AvailableWorkspace = {
   numerosIndisponiveis: number[],
   funcionarioFinal: {
      id: number,
      nome: string
   }[],
   funcionarioArmador: {
      id: number,
      nome: string
   }[],
   funcionarioAcenteiro: {
      id: number,
      nome: string
   }[],
   funcionarioEncosteiro: {
      id: number,
      nome: string
   }[],
}